# M0-9 — Identify what "Codex" is on this machine

**Outcome:** PASS
**Run:** 2026-07-25T16:34:01Z

## Pass condition

A written finding. If it is a CLI, its isolation mechanism is CODEX_HOME, which is Provider B and out of V1 scope.

## Result

Codex on this machine is a command line tool at /Applications/ChatGPT.app/Contents/Resources/codex, and it is supported.

## Findings

- It has no Dock tile and no Chromium profile, so neither half of the Electron mechanism applies. Its whole session — credentials, configuration, history — lives under one directory named by `CODEX_HOME`.
- The original spec wrote this off as "Provider B, not V1". That was a statement about how the product had been framed, not about the problem: a tool with a single config directory is *easier* to run two accounts of than any Electron app, because there is no bundle to clone and no signature to rebuild. It is implemented, and each instance opens a terminal session with its own CODEX_HOME.
- Verified directly: with the user's default session signed in, an instance reports `Not logged in` — the redirection takes effect.

## Evidence

```
# found config directory at /Users/example/.codex

$ mal inspect codex
# path: /Applications/ChatGPT.app/Contents/Resources/codex
# version: 0.146.0-alpha.3.1
# tier: supported, mechanism: configEnvironment
```

---

Machine: Mac16,8 · arm64 · Version 26.5.2 (Build 25F84)
Primary application: Claude 1.24012.9 (com.anthropic.claudefordesktop)
Second application: Kimi.app
Generated by `mal m0`. Re-run with: `mal m0 --only 9`
